import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ActivitiesService {
  async getActivities(itineraryId: number) {
    // Replace with backend API call
    return [];
  }
}